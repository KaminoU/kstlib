# Example secrets file - encrypt with: kstlib secrets encrypt secrets.example.yml
# Only fields matching the encrypted_regex will be encrypted (key, secret, token, etc.)

binance:
  mainnet:
    api_key: "your_binance_mainnet_api_key_here"
    private_key: "your_binance_mainnet_private_key_here"
  testnet:
    api_key: "your_binance_testnet_api_key_here"
    private_key: "your_binance_testnet_private_key_here"

# This field will NOT be encrypted (doesn't match regex)
bot_name: "astro"
environment: "production"
