# Example secrets file - encrypt with: sops -e -i secrets.example.yml
# Only fields matching the encrypted_regex will be encrypted (key, secret, token, etc.)

binance:
  api_key: "your_binance_api_key_here"
  api_secret: "your_binance_api_secret_here"

kraken:
  api_key: "your_kraken_api_key_here"
  api_secret: "your_kraken_api_secret_here"

# This field will NOT be encrypted (doesn't match regex)
bot_name: "astro"
environment: "production"
