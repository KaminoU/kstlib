# kstlib configuration for GitHub RAPI example
# Run from this directory: python demo.py

rapi:
  include:
    - "./*.rapi.yml"  # Auto-discover all *.rapi.yml in current directory

logging:
  level: INFO
  format: "%(asctime)s | %(levelname)-8s | %(message)s"
