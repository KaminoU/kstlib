app:
  name: "Base Application"
  version: "1.0.0"
  debug: true
  allowed_hosts:
    - "localhost"
    - "127.0.0.1"

database:
  host: "localhost"
  port: 5432
  name: "mydb"
  pool_size: 10

cache:
  backend:
    host: "localhost"
    port: 6379
