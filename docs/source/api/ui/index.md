# UI Helpers

Declarative wrappers around Rich/Textual components. These helpers keep layout decisions in config so
dashboards, watchdog panels, and status boards stay consistent without reading the underlying UI code.

```{toctree}
:maxdepth: 1

panels
spinner
tables
```
